@model IEnumerable<ProjectApplication.Models.OrderViewIndex>

@{
    ViewBag.Title = "Quản lý đơn hàng";
}

<h2>Quản lý đơn hàng</h2>

<!-- 🔘 Nút chuyển nhanh -->
<div class="mb-3">
    <button id="btnManagerProduct" class="btn btn-primary me-2">Quản lý sản phẩm</button>
    <button id="btnDiscount" class="btn btn-primary me-2">Chương trình khuyến mãi</button>
    <button id="btnOrder" class="btn btn-primary me-2">Đơn hàng</button>
</div>

<!-- 🧾 Danh sách đơn hàng -->
<table class="table table-bordered table-striped align-middle">
    <thead class="table-light">
        <tr>
            <th>Tên Khách hàng</th>
            <th>Số điện thoại</th>
            <th>Ngày đặt</th>
            <th>Trạng thái</th>
            <th>Tổng tiền</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@Html.ActionLink(order.Name, "Details", "OrderManager", new { id = order.Id }, new { @class = "text-decoration-none fw-bold" })</td>
                <td>@order.Phone</td>
                <td>@order.OrderDate.ToString("dd/MM/yyyy")</td>

                <!-- Badge trạng thái -->
                <td>
                    @if (order.OrderStatus == "Pending")
                    {
                        <span class="badge bg-warning text-dark">Đang chờ xác nhận</span>
                    }
                    else if (order.OrderStatus == "Processing")
                    {
                        <span class="badge bg-info text-dark">Đang xử lý</span>
                    }
                    else if (order.OrderStatus == "Delivered")
                    {
                        <span class="badge bg-success">Đã giao</span>
                    }
                    else if (order.OrderStatus == "Cancelled")
                    {
                        <span class="badge bg-danger">Đã hủy</span>
                    }
                </td>

                <!-- Tổng tiền -->
                <td>@order.TotalAmount.ToString("N0") ₫</td>

                <!-- Hành động -->
                <td>
                    @if (order.OrderStatus == "Pending")
                    {
                        <form action="@Url.Action("MarkAsProcessing", "OrderManager", new { id = order.Id })" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-success btn-sm">Xác nhận</button>
                        </form>
                        @Html.ActionLink("Chi tiết", "Details", "OrderManager", new { id = order.Id }, new { @class = "btn btn-outline-info btn-sm me-2" })
                    }
                    else if (order.OrderStatus == "Processing")
                    {
                        <form action="@Url.Action("MarkAsDelivered", "OrderManager", new { id = order.Id })" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-primary btn-sm">Đã giao</button>
                        </form>
                        @Html.ActionLink("Chi tiết", "Details", "OrderManager", new { id = order.Id }, new { @class = "btn btn-outline-info btn-sm me-2" })
                    }
                    else if (order.OrderStatus == "Cancelled")
                    {
                        @Html.ActionLink("Chi tiết", "Details", "OrderManager", new { id = order.Id }, new { @class = "btn btn-outline-info btn-sm me-2" })
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- 🔗 Liên kết JS -->
@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $("#btnManagerProduct").click(function () {
                window.location.href = '@Url.Action("Index", "ProductManager")';
            });

            $("#btnDiscount").click(function () {
                window.location.href = '@Url.Action("Index", "DiscountManager")';
            });
            $("#btnOrder").click(function () {
                window.location.href = '@Url.Action("Index", "OrderManager")';
            });
        });
    </script>
}
